# ── Core ──
pydantic>=2.5.0,<2.11        # 2.11+ 的 pydantic-core 与 gradio 5.x 冲突
pyyaml>=6.0
rank-bm25>=0.2.2

# ── UI ──
# gradio 4.x 与 huggingface_hub>=1.0 不兼容（HfFolder 已移除）
# gradio 5.12 有 pydantic schema bool bug
# gradio 6.x 改了 Blocks 构造参数，会 break css/theme
# → 锁定 5.14–5.x
gradio>=5.14.0,<6.0
gradio-client>=1.5.0,<2.0

# ── Embeddings / Rerank ──
sentence-transformers>=3.0.0,<4.0
FlagEmbedding>=1.2.10
huggingface-hub>=1.0.0,<2.0
tokenizers>=0.20.0

# ── LLM ──
# transformers >=4.48 强制要求 torch>=2.6（CVE-2025-32434）
# 若无法装 torch>=2.6，降级到 transformers==4.47.1 可绕过
transformers>=4.48.0,<5.0
openai>=1.30.0

# ── Vector DB ──
pymilvus>=2.4.6,<3.0

# ── Parsing（多模态文档解析）──
pymupdf>=1.24.0
pdfplumber>=0.11.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2
beautifulsoup4>=4.12.0
lxml>=5.0.0
Pillow>=10.0.0,<13.0

# ── OCR（可选，PDF/图片解析用）──
pytesseract>=0.3.10
# paddleocr>=2.7.0            # 备选 OCR 引擎

# ── ASR 语音识别（可选，不装则语音输入不可用）──
# faster-whisper>=1.0.0       # 取消注释即可启用

# ── API 服务（可选）──
fastapi>=0.110.0
uvicorn>=0.27.0

# ── 工具库 ──
numpy>=1.24.0,<3.0
pandas>=2.0.0,<3.0
tqdm>=4.60.0
httpx>=0.27.0
orjson>=3.9.0
regex>=2023.0
jieba>=0.42.1
